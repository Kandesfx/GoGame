# fly.toml app configuration file for gogame-backend
# File này ở root directory để deploy backend

app = 'gogame-backend'
primary_region = 'sin'

[build]
  dockerfile = 'Dockerfile.backend'
  dockerfile_context = '.'

[deploy]
  # Tạm thời tắt release_command (migrations sẽ chạy thủ công nếu cần)
  # release_command = 'alembic -c /app/alembic.ini upgrade head'

[env]
  DEBUG = 'false'
  # DATABASE_URL is automatically set by Fly.io when PostgreSQL is attached
  # Code will read directly from DATABASE_URL environment variable

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'

[[vm]]
  memory = '2gb'
  cpu_kind = 'shared'
  cpus = 1

